{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "require-corp, not credentialless. credentialless is Chromium-only - Safari implements",
  "//2": "only require-corp, so a page sending credentialless never becomes cross-origin isolated",
  "//3": "there, the browser withholds SharedArrayBuffer, and the gate correctly reports it as",
  "//4": "missing. That looked like Safari lacking the feature; Safari has had it since 2021.",
  "//5": "Every subresource here is same-origin except googletagmanager, which sends both",
  "//6": "Access-Control-Allow-Origin and Cross-Origin-Resource-Policy, so it satisfies the policy.",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        },
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        }
      ]
    }
  ],
  "//7": "The old apex still resolves and still points here, so it is answered with a permanent redirect rather than a second copy of the site: one canonical host, and every existing link keeps working.",
  "redirects": [
    {
      "source": "/(.*)",
      "has": [
        {
          "type": "host",
          "value": "vicecity.wasm.com.br"
        }
      ],
      "destination": "https://revc.wasm.ltd/$1",
      "permanent": true
    }
  ]
}
